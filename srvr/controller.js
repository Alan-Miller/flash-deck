// Works, but is not currently used

// module.exports = {
//     getAllCards: (req, res) => {
//         req.app.get('db').get_allCards([req.params.id])
//         .then(response => {
//             res.status(200).send(response);
//         });
//     }

// }